#include <stdio.h>

int main() {

int idade = 25;// Declara uma variável inteira chamada "idade"
float altura = 1.75; // Declara uma variável de ponto flutuante chamada "altura"
char opcao = 'S'; // Declara uma variável do tipo caractere chamada "letra"
char nome[20] = "Sergio"; // Declara um array de 20 caracteres e inicializa com "Sergio"


printf("A idade do %s é: %d\n", nome, idade);
printf("A altura é: %f", altura);
printf("A opção é: %c\n", opcao);

/*
printf("%formato1, %formato2, %formato3, varialvel1, variavel2, variavel3")
%d: Imprime um inteiro em formato decimal.
%i: Equivalente a %d.
%f: Imprime um número de ponto flutuante no formato padrão.
%e: Imprime um número de ponto flutuante na notação científica.
%c: Imprime um único caractere.
%s: Imprime uma cadeia (string) de caracteres.
*/




}